post_install() {
  systemctl enable --now bluetooth
  systemctl enable --now lightdm

  post_upgrade
}

post_upgrade() {
    cp /etc/lightdm.conf /etc/lightdm.conf.pacsave
    mv /etc/lightdm.conf.pacnew.install /etc/lightdm.conf
}
